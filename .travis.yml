language: java

jdk:
  - openjdk10

script:
  - git clone https://github.com/eclipse/kura
  - java -jar /home/travis/build/SpoonLabs/sorald/target/sorald-1.1-SNAPSHOT-jar-with-dependencies.jar --originalFilesPath kura --ruleKeys 2184
  - mvn test